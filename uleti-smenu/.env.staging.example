# Copy to `.env.staging` for staging builds (do not commit real `.env.staging`).
# Usage: copy to `.env` before `npm run build`, or use your CI secret.

VITE_API_BASE_URL=https://uletismenu-api-staging.onrender.com
