import styles from "../utils/loader.module.scss";

const Loader = () => (
  <div className={styles.loaderContainer}>
    <div className={styles.loader}>dsadasdsaasd</div>
  </div>
);

export default Loader;