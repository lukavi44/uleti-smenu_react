# Copy to `.env.test` for TEST builds (do not commit real `.env.test`).
# Usage: copy to `.env` before `npm run build`, or set in Cloudflare Pages (develop branch).

VITE_API_BASE_URL=https://api-test.uletismenu.com
